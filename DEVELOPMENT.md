
## Role Context Manager Setup

1. Install RCM (v1.7.0+)
2. Clone repository
3. Run: `/validate-setup` (should automatically detect paths)
4. Run: `/load-role-context` to load your role

Configuration is stored in `.myorg-rcm/paths.json`
